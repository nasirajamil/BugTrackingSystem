<!DOCTYPE html>
<html lang="en">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <head>
    <title>
      Signup
    </title>
    <link rel="stylesheet"  href="users.scss">
  </head>
  <body>
  <h1 class="myborder">BUG TRACKING SYSTEM</h1>

  <ul>
    <li><%= link_to "View Projects", project_index_path%></li>
    <li><%= link_to "Create Projects", new_project_path%></li>
  </ul>

  <center><h2>All projects</h2></center>

  <table border=1>
    <tr>
      <th>Name</th>
      <th>Desc</th>
      <th>Start_date</th>
      <th>End_date</th>
      <th colspan="3">Actions</th>
    </tr>

    <% @projects.each do |p| %>
      <tr>
        <td style="width:100px"><%= p.name %></td>
        <td style="width:300px"><%= p.desc %></td>
        <td style="width:300px"><%= p.start_date %></td>
        <td style="width:300px"><%= p.end_date %></td>
        <td class="actions buttons"><%= link_to 'Show', project_path(p) %></td>
        <td class="actions buttons"><%= link_to 'Edit', edit_project_path(p) %></td>
        <td class="actions buttons"><%= link_to 'Destroy', project_path(p),
                                                method: :delete,
                                                data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </table>
  <br>

  </body>
</html>