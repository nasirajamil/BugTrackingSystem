<!DOCTYPE html>
<html lang="en">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <head>
    <title>
      Signup
    </title>
    <link rel="stylesheet"  href="users.scss">
  </head>
  <h1 class="myborder">BUG TRACKING SYSTEM</h1>

  <ul>
    <li><%= link_to "Sign in", new_session_path(resource_name)%></li>
    <li><%= link_to "Sign_up", new_registration_path(resource_name)%></li>
<!--    <li><%#= link_to "Reset Password", new_password_path(resource_name)%></li>-->
<!--    <li><%#= link_to "Edit Profile", edit_user_registration_path(resource_name)%></li>-->
  </ul>

  <center><h2>Login</h2></center>
  <div class="container">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

      <% if resource.errors.any? %>
        <div id="error_explanation">
          <ul>
            <% resource.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= render "devise/shared/error_messages", resource: @user %>
      <div class="field">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "field_len" %>
      </div>
      <br>
      <div class="field">
        <%= f.label :password %><%= link_to "forgot password?", new_password_path(resource_name),class: "password" %><br />
        <%= f.password_field :password, autocomplete: "current-password",class: "field_len" %>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="field">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me %>
        </div>
      <% end %>
      <br>
      <div class="actions">
        <%= f.submit "Log in", class: "mybutton"%>
      </div>
      <br>
      <small>Don't Have an account yet?</small>
      <%= link_to "Sign_up", new_registration_path(resource_name), class: "mylink"%>
    <% end %>



  </div>
</html>